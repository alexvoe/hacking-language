

// Sound
var x = document.getElementById("Audio"); 

function playAudio() { 
  x.play(); 
} 

function pauseAudio() { 
  x.pause(); 
} 




// ?!
document.getElementById('fragezeichen').addEventListener('keypress', function (event) {
  // var regex = new RegExp("^[a-zA-Z0-9]+$");
  var key = String.fromCharCode(!event.charCode ? event.which : event.charCode);
  if (key === '?' || key === '!') {
    console.log(key)
  } else {
    event.preventDefault();
    return false;

  }
});


document.getElementById('fragezeichen_2').addEventListener('keypress', function (event) {
  // var regex = new RegExp("^[a-zA-Z0-9]+$");
  var key = String.fromCharCode(!event.charCode ? event.which : event.charCode);
  if (key === '?' || key === '!') {
    console.log(key)
  } else {
    event.preventDefault();
    return false;

  }
});

document.getElementById('fragezeichen_3' ).addEventListener('keypress', function (event) {
  // var regex = new RegExp("^[a-zA-Z0-9]+$");
  var key = String.fromCharCode(!event.charCode ? event.which : event.charCode);
  if (key === '?' || key === '!') {
    console.log(key)
  } else {
    event.preventDefault();
    return false;

  }
});

document.getElementById('fragezeichen_4' ).addEventListener('keypress', function (event) {
  // var regex = new RegExp("^[a-zA-Z0-9]+$");
  var key = String.fromCharCode(!event.charCode ? event.which : event.charCode);
  if (key === '?' || key === '!') {
    console.log(key)
  } else {
    event.preventDefault();
    return false;

  }
});

document.getElementById('fragezeichen_5' ).addEventListener('keypress', function (event) {
  // var regex = new RegExp("^[a-zA-Z0-9]+$");
  var key = String.fromCharCode(!event.charCode ? event.which : event.charCode);
  if (key === '?' || key === '!') {
    console.log(key)
  } else {
    event.preventDefault();
    return false;

  }
});


document.getElementById('fragezeichen_6' ).addEventListener('keypress', function (event) {
  // var regex = new RegExp("^[a-zA-Z0-9]+$");
  var key = String.fromCharCode(!event.charCode ? event.which : event.charCode);
  if (key === '?' || key === '!') {
    console.log(key)
  } else {
    event.preventDefault();
    return false;

  }
});

document.getElementById('fragezeichen_7' ).addEventListener('keypress', function (event) {
  // var regex = new RegExp("^[a-zA-Z0-9]+$");
  var key = String.fromCharCode(!event.charCode ? event.which : event.charCode);
  if (key === '?' || key === '!') {
    console.log(key)
  } else {
    event.preventDefault();
    return false;

  }
});


document.getElementById('fragezeichen_8' ).addEventListener('keypress', function (event) {
  // var regex = new RegExp("^[a-zA-Z0-9]+$");
  var key = String.fromCharCode(!event.charCode ? event.which : event.charCode);
  if (key === '?' || key === '!') {
    console.log(key)
  } else {
    event.preventDefault();
    return false;

  }
});

document.getElementById('fragezeichen_9' ).addEventListener('keypress', function (event) {
  // var regex = new RegExp("^[a-zA-Z0-9]+$");
  var key = String.fromCharCode(!event.charCode ? event.which : event.charCode);
  if (key === '?' || key === '!') {
    console.log(key)
  } else {
    event.preventDefault();
    return false;

  }
});

document.getElementById('fragezeichen_10' ).addEventListener('keypress', function (event) {
  // var regex = new RegExp("^[a-zA-Z0-9]+$");
  var key = String.fromCharCode(!event.charCode ? event.which : event.charCode);
  if (key === '?' || key === '!') {
    console.log(key)
  } else {
    event.preventDefault();
    return false;

  }
});


document.getElementById('fragezeichen_11' ).addEventListener('keypress', function (event) {
  // var regex = new RegExp("^[a-zA-Z0-9]+$");
  var key = String.fromCharCode(!event.charCode ? event.which : event.charCode);
  if (key === '?' || key === '!') {
    console.log(key)
  } else {
    event.preventDefault();
    return false;

  }
});


document.getElementById('fragezeichen_12' ).addEventListener('keypress', function (event) {
  // var regex = new RegExp("^[a-zA-Z0-9]+$");
  var key = String.fromCharCode(!event.charCode ? event.which : event.charCode);
  if (key === '?' || key === '!') {
    console.log(key)
  } else {
    event.preventDefault();
    return false;

  }
});


document.getElementById('fragezeichen_13' ).addEventListener('keypress', function (event) {
  // var regex = new RegExp("^[a-zA-Z0-9]+$");
  var key = String.fromCharCode(!event.charCode ? event.which : event.charCode);
  if (key === '?' || key === '!') {
    console.log(key)
  } else {
    event.preventDefault();
    return false;

  }
});


//popups
// When the user clicks on div, open the popup
function myFunction() {
  var popup = document.getElementById('myPopup');
  popup.classList.toggle('show');
}


